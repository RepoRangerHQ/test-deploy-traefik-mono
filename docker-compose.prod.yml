services:
  # --- Traefik Service (Reverse Proxy) ---
  traefik:
    image: traefik:v2.10
    container_name: traefik
    restart: always
    command:
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      # Redirect HTTP -> HTTPS
      - "--entrypoints.web.http.redirections.entryPoint.to=websecure"
      - "--entrypoints.web.http.redirections.entryPoint.scheme=https"
      # Let's Encrypt (uncomment ถ้าไม่ใช้ Cloudflare Proxy)
      # - "--certificatesresolvers.letsencrypt.acme.email=your-email@example.com"
      # - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      # - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # - ./letsencrypt:/letsencrypt  # สำหรับ Let's Encrypt
    networks:
      - app-network

  # --- Frontend (Vue.js) ---
  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: https://${BACKEND_DOMAIN}
    container_name: vue-frontend
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=app-network"
      
      # Domain จาก .env file
      - "traefik.http.routers.frontend.rule=Host(`${FRONTEND_DOMAIN}`) || Host(`www.${FRONTEND_DOMAIN}`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls=true"
      # - "traefik.http.routers.frontend.tls.certresolver=letsencrypt"
      
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"
    networks:
      - app-network

  # --- Backend (Hono API) ---
  backend:
    build: ./backend
    container_name: hono-backend
    restart: always
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=app-network"
      
      # Domain จาก .env file
      - "traefik.http.routers.backend.rule=Host(`${BACKEND_DOMAIN}`)"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls=true"
      # - "traefik.http.routers.backend.tls.certresolver=letsencrypt"
      
      - "traefik.http.services.backend.loadbalancer.server.port=4000"
    networks:
      - app-network

networks:
  app-network:
    name: app-network
    driver: bridge

