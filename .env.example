# === Domain Configuration ===
FRONTEND_DOMAIN=vue.devng.pro
BACKEND_DOMAIN=apivue.devng.pro

# === API URL (for frontend build) ===
# Production จะใช้ https://${BACKEND_DOMAIN} อัตโนมัติ

# === Optional: Let's Encrypt ===
# ACME_EMAIL=your-email@example.com

